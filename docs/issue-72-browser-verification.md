# Issue #72 continuous browser verification

## Environment

- Branch base: `origin/main@fc335f670c8b666b2ed49d849410dc210143d1dd`
- Local-only Compose project: `smartpark-issue72`
- Frontend: `http://127.0.0.1:15173/`
- Backend: `http://127.0.0.1:15174/`
- Analytics PostgreSQL and time-parser were healthy during the run.
- Approved source: `smart-park-ai-approved-designs.zip / 04-operations-reports.png`, 1672 x 941, SHA-256 `621EB96B910B86F4BFBA75609A3557B3CFF4AB4EE5C9DC2FA80F0CF9CCD17F3F`.

## Continuous path

The path was executed in one in-app browser session against the locally built branch:

1. Opened the unique customer shell and selected `运营报告`.
2. Confirmed history was empty and no report was generated by entering the page.
3. Kept `近 5 天` selected and clicked `立即生成` once.
4. Observed the persisted report progress from `GENERATING` to `COMPLETED`.
5. Recorded report ID `089421b0-2130-424f-8ade-cd35b286203e`, run ID `91eb949a-247d-4b4c-b4aa-ca291c1531d5` and artifact ID `75d1b869-0285-45f8-8c4c-1b2dadbca066`.
6. Used the page `刷新` action; preview and history still showed the same report ID.
7. Performed a browser reload, returned to `运营报告`, and expanded the snapshot metadata; the same report ID was restored from history without a create request.
8. Left for `园区总览`, returned to `运营报告`, and explicitly reopened the one history row; the summary and report ID were unchanged.
9. Clicked `下载报告（Markdown）`. Browser network evidence recorded `GET /api/operations-reports/089421b0-2130-424f-8ade-cd35b286203e/download` with HTTP 200.
10. Downloaded the same endpoint independently and computed SHA-256 `ED31F955266EF2B1C071402C8BC804D70E485C6AEB4B1CDFB77FEBFEB8322718`, exactly matching the artifact checksum in the selected report receipt.
11. Re-read history after the sequence: `totalElements=1`, selected ID unchanged, status `COMPLETED`. Therefore refresh, leave-return, history open and download did not generate another report.

## Screenshot evidence

| Evidence | Purpose |
| --- | --- |
| `docs/evidence/issue-72/operations-reports-1672x941.png` | Same-size comparison with the approved 1672 x 941 source. |
| `docs/evidence/issue-72/operations-reports-continuous-proof.png` | Full report after refresh, leave-return, history reopen and download; includes snapshot ID and all persisted sections. |
| `docs/evidence/issue-72/operations-reports-1366x768.png` | Common desktop viewport; primary action, current history, chart and download remain usable. |
| `docs/evidence/issue-72/operations-reports-820x900.png` | Narrow layout stacks the preview and rail without horizontal document overflow. |

The 1672 and 1366 views reported document widths smaller than their CSS viewport widths. The 820 view reported `scrollWidth=805` for `innerWidth=820`. No browser console error was emitted during reload and re-entry.

## Automated checks

- Backend: `./mvnw.cmd test` -> 1328 tests passed, 3 existing conditional tests skipped.
- Frontend: `npm.cmd run test:unit` -> 49 files / 505 tests passed.
- Frontend typecheck: passed.
- Frontend production build: passed; the existing Vite chunk-size advisory remains non-blocking and was not expanded into this issue.
