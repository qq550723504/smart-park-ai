# Issue #73 四页连续演示脚本

## 演示前提

- 客户入口：`http://127.0.0.1:5273/`
- 隔离 Compose 项目：`smartpark-issue73`
- 能力快照：运营分析已启用、客服回答使用在线模型、知识检索使用现有 RAG；语音能力关闭。
- 页面中的园区、能耗、告警和设备是确定性演示数据；AI 调用、报告生成与告警工作流调用真实现有后端。

## 连续路径

1. 园区总览
   - 确认统一顶部导航、演示数据标签、四类指标、楼宇示意和趋势卡片。
   - 选择 `B2 · 研发大厦`，点击“查看分析”。
2. 运营分析
   - 核对楼宇仍为 B2，能耗周期为 `2026-09-09T13:00:00Z` 至 `2026-09-10T13:00:00Z`。
   - 点击“运行 AI 分析”。预期页面显示非空“AI 分析结论”，本次在线结果为 B2 能耗基线偏差率 `23.68%`。
   - 在线运行只做受控只读查询，不执行设备控制。
3. 事件与工单
   - 返回总览，选择 `B1 · 创新中心`；等待同一楼宇事件依据完成，再进入分析和人工确认。
   - 核对告警 `ALT-ORCH-ENERGY-B1-001`，确认弹窗后调用现有告警工作流。
   - 预期得到幂等实际回执 `WO-0001`、状态“已创建，待处理”；页面明确说明建单不等于解决。
4. 运营报告
   - 点击“继续查看运营报告”。这只进入报告中心，不声明既有报告已纳入该工单。
   - 打开历史快照或显式生成日报。实际验收报告 `4a639c1c-bf1e-4576-bb0f-577bc7660572` 为 `COMPLETED`，Markdown 下载可用。
5. AI 助手
   - 任意页面点击顶部“AI 助手”。推荐问题只填充草稿，确认发送前没有请求。
   - 停车咨询通过现有客服接口返回回答与“访客停车指南”来源；知识不足时按后端真实结果展示客服工单和“尚未解决”边界。
   - 关闭后焦点回到顶部入口；再次打开或切页时保留当前会话和未发送草稿。
6. 重开导览
   - 点击“重开导览”，阅读确认说明后执行。
   - 预期回到总览并清除本地选择/草稿/助手展示；`WO-0001`、历史报告和运行数据仍保留。

## 失败或缺失时怎么演示

- 在线分析失败：展示可重试提示，继续阅读已经取得的事实与图表，不回放旧成功结论。
- 没有合法告警标识：工单按钮禁用并明确原因，不拼接另一个告警。
- 客服知识不足或模型不可用：显示人工接入、真实客服工单或可重试提示，不使用默认成功话术。
- 报告无历史、部分完成、失败或下载不可用：沿用 #77/#78 的准确中文状态，不生成替代内容。
