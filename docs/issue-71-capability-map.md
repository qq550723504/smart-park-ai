# Issue #71 事件与工单能力映射

## 本轮边界

本页只复用现有客户框架、分析证据、告警工作流、人工审批和演示工单端口。运营报告、AI 助手、催办、派发、完工、部署和 Issue #71 合并均不在本轮范围。

## 同一事件映射

| 字段 | 运营分析证据 | 可建单告警 | 结论 |
| --- | --- | --- | --- |
| 告警编号 | `ALT-ORCH-ENERGY-B1-001` | `ALT-ORCH-ENERGY-B1-001` | 相同 |
| 楼宇 | `B1` | `B1` | 相同 |
| 设备 | `DEV-ENERGY-B1-001` | `DEV-ENERGY-B1-001` | 相同 |
| 类别 | `ENERGY` | `ENERGY` | 相同 |
| 风险等级 | `HIGH` | `HIGH` | 相同，进入现有人工审批闸门 |

客户页先读取分析窗口内的原始告警，再通过只读 `GET /api/alerts/{alertId}` 核验现有告警端口中的可建单对象。只有告警编号、楼宇、设备和类别全部一致时才开放人工确认；任一字段不一致、证据不可用或对象不存在都会阻止建单，并明确提示不会替换成另一条告警。

`ALT-TEMP-001` 不能作为替代对象：分析证据把它关联到 `B1 / AC-B1-07`，现有工作流夹具却是 `A1 / DEV-HVAC-001`。本轮保留该不一致为拒绝条件，没有用它拼接成功链路。

## 复用的现有能力

1. `CustomerShell`：园区总览、运营分析、事件与工单共用唯一客户导航和园区横幅。
2. `CustomerAnalysisContext`：从分析页携带同一楼宇、异常窗口和 `anomalyId`，不在工单页重新挑选对象。
3. `POST /api/alerts/{alertId}/workflows`：启动现有告警工作流；同一告警对运行中、待审批、完成或拒绝结果保持幂等，仅在权威状态为 `FAILED` / `WORK_ORDER_FAILED` 且没有工单回执时，由用户明确操作触发现有失败重试契约。
4. `POST /api/workflows/{workflowId}/approval`：风险闸门暂停后由当前演示角色提交人工决定。
5. `GET /api/workflows/{workflowId}`：审批响应不明确、审批 409 冲突或页面内刷新时只读回查权威状态，避免重复人工决定；回查结果同时校验请求 generation 与 workflowId。读取失败会保留同一事件的已知结果、阻止再次审批，并明确要求先刷新状态。
6. 现有 `WorkOrderPort`：返回实际演示工单编号和状态；浏览器验证得到 `WO-0001 / PENDING_EXECUTION`。

## 真实性边界

- `PENDING_EXECUTION` 映射为“已创建，待处理”，绝不映射为“已解决”。
- 页面明确写出“创建工单不表示问题已解决”。
- 未由后端提供的责任部门、负责人和平均处理时长显示“未提供”，不生成假负责人、假 SLA 或假均值。
- 处理建议来自分析页已有的受治理上下文；它们是人工核查建议，不是故障定论，也不表示已经执行。
- 证据加载与工作流回执使用独立生命周期：页面内刷新重取证据和已知 workflow 状态，但不会清空同一事件已经取得的工单回执；初始证据加载被离开操作中断时，返回同一事件会重新请求，旧响应不会覆盖新结果。
- 现有工单适配器和工作流执行存储是演示内存实现。浏览器硬刷新或进程重启后的跨会话恢复仍需要独立持久化与查询契约，未在本轮伪造。
